{
  "name": "asbl-getout",
  "version": "1.0.0",
  "description": "GET OUT — ASBL events discovery app (backend + interactive frontend) built from Figma design",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
